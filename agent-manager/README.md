# 🤖 Agent 管理系统

OpenClaw Agent 可视化管理平台，用于集中管理、配置和监控多个 AI Agent。

## 📋 功能特性

### ✅ 已实现
- [x] Agent 仪表盘（总数、活跃、空闲统计）
- [x] Agent 列表展示
- [x] 创建新 Agent
- [x] 任务管理（查看、创建）
- [x] 实时状态显示
- [x] 响应式 UI 设计

### 🚧 开发中
- [ ] 编辑 Agent 配置
- [ ] 删除 Agent
- [ ] 性能分析图表
- [ ] OpenClaw 集成
- [ ] 数据库持久化
- [ ] 批量操作

## 🏗️ 技术栈

**前端**
- React 18
- React Router
- Tailwind CSS
- Vite

**后端**
- Node.js
- Express
- SQLite (计划中)

## 🚀 快速开始

### 1. 安装依赖

```bash
# 安装所有依赖
npm run install:all
```

或者分别安装：

```bash
# 安装根目录依赖
npm install

# 安装前端依赖
cd frontend && npm install

# 安装后端依赖
cd backend && npm install
```

### 2. 启动开发服务器

```bash
# 同时启动前后端
npm run dev
```

或者分别启动：

```bash
# 启动前端（端口 3000）
cd frontend && npm run dev

# 启动后端（端口 3001）
cd backend && npm run dev
```

### 3. 访问应用

打开浏览器访问：http://localhost:3000

## 📁 项目结构

```
agent-manager/
├── frontend/              # 前端 React 应用
│   ├── src/
│   │   ├── components/    # 可复用组件
│   │   ├── pages/         # 页面组件
│   │   │   ├── Dashboard.jsx
│   │   │   ├── Agents.jsx
│   │   │   ├── Tasks.jsx
│   │   │   └── Analytics.jsx
│   │   ├── services/      # API 服务
│   │   ├── styles/        # 样式文件
│   │   ├── App.jsx        # 主应用
│   │   └── main.jsx       # 入口文件
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js
│   └── tailwind.config.js
├── backend/               # 后端 Express API
│   ├── server.js          # 主服务器
│   └── package.json
├── package.json           # 根配置
└── README.md
```

## 🎯 当前页面

### 1. 仪表盘 (/)
- Agent 统计概览
- 快速操作入口
- 最近活动动态

### 2. Agent 管理 (/agents)
- Agent 列表展示
- 创建新 Agent
- 查看/编辑 Agent
- 分配任务

### 3. 任务管理 (/tasks)
- 当前任务列表
- 任务进度追踪
- 创建新任务

### 4. 数据分析 (/analytics)
- Token 使用统计（开发中）
- 成本分析（开发中）

## 🔌 API 接口

### Agent 相关
```
GET    /api/agents          # 获取所有 Agent
POST   /api/agents          # 创建新 Agent
GET    /api/agents/:id      # 获取单个 Agent
PUT    /api/agents/:id      # 更新 Agent
DELETE /api/agents/:id      # 删除 Agent
```

### 任务相关
```
GET    /api/tasks           # 获取任务列表
POST   /api/tasks           # 创建新任务
```

### 系统
```
GET    /api/stats           # 获取统计数据
GET    /api/health          # 健康检查
```

## 📝 下一步计划

### 第 1 周（MVP）
- [x] 项目初始化
- [x] 基础 UI 框架
- [x] Agent 列表展示
- [x] 创建 Agent 功能
- [ ] 连接 OpenClaw

### 第 2 周（核心功能）
- [ ] 任务管理系统
- [ ] Agent 配置编辑
- [ ] 实时状态更新
- [ ] 基础数据统计

### 第 3 周（增强功能）
- [ ] 性能分析图表
- [ ] 任务历史记录
- [ ] 批量操作
- [ ] 数据导出

### 第 4 周（优化部署）
- [ ] UI/UX 优化
- [ ] 性能优化
- [ ] 部署配置
- [ ] 文档完善

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License